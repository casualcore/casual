---
server:
  default:
    service:
      transaction: join
      category: some.category


  services:
    - name: s1

    - name: s2
      transaction: auto

    - name: s3
      function: f3

    - name: s4
      function: f4
      category: some.other.category


