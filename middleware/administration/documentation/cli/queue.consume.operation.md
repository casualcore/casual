# casual queue --consume

[//]: # (Attention! this is a generated markdown from casual-administration-cli-documentation - do not edit this file!)

```console
host# casual --help queue --consume

--consume [0..1]  (<queue>, <count>) [1..2]
     consumes messages from the provided `queue` and send it downstream
     
     @note: part of casual-pipe

     Example:
     
        casual queue --consume a > /dev/null
     
        casual queue --consume a 10 | casual queue --enqueue b
     
        casual transaction --begin \
           | casual queue --consume a \
           | casual queue --enqueue b \
           | casual transaction --commit

```
