package { 'uuid':
	name => 'uuid-dev',
	ensure => installed
}
package { 'yaml':
	name => 'libyaml-cpp0.3-dev',
	ensure => installed
}
package { 'json':
	name => 'libjson-c-dev',
	ensure => installed
}
package { 'sqlite3':
	name => 'libsqlite3-dev',
	ensure => installed
}

