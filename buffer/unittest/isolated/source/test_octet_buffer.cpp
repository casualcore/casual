//
// test_casual_order_buffer.cpp
//
//  Created on: 3 nov 2013
//      Author: Kristone
//


#include <gtest/gtest.h>

#include "buffer/octet.h"


/*
TEST( casual_octet_buffer, allocate_with_normal_size__expecting_success)
{
   char buffer[64];
   EXPECT_TRUE( CasualOctetCreate( buffer, sizeof(buffer)) == CASUAL_OCTET_SUCCESS);
}

TEST( casual_octet_buffer, allocate_with_zero_size__expecting_success)
{
   char buffer[0];
   EXPECT_TRUE( CasualOctetCreate( buffer, sizeof(buffer)) == CASUAL_OCTET_SUCCESS);
}

TEST( casual_octet_buffer, reallocate_with_less_size__expecting_success)
{
   char buffer[64];
   EXPECT_TRUE( CasualOctetCreate( buffer, sizeof(buffer)) == CASUAL_OCTET_SUCCESS);
   EXPECT_TRUE( CasualOctetReduce( buffer, sizeof(buffer) / 2) == CASUAL_OCTET_SUCCESS);
}

TEST( casual_octet_buffer, reallocate_with_zero_size__expecting_success)
{
   char buffer[64];
   EXPECT_TRUE( CasualOctetCreate( buffer, sizeof(buffer)) == CASUAL_OCTET_SUCCESS);
   EXPECT_TRUE( CasualOctetReduce( buffer, 0) == CASUAL_OCTET_SUCCESS);
}


TEST( casual_octet_buffer, reallocate_with_greater_size__expecting_success)
{
   char buffer[64];
   EXPECT_TRUE( CasualOctetCreate( buffer, sizeof(buffer) / 2) == CASUAL_OCTET_SUCCESS);
   EXPECT_TRUE( CasualOctetReduce( buffer, sizeof(buffer)) == CASUAL_OCTET_SUCCESS);
}
*/

